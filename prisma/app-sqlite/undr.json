{
  "$schema": "https://app.underscore.is/schemas/undr.json",
  "scripts": {
    "install": "npm ci && npx prisma generate",
    "dev": "npx ts-node example.ts",
    "build": "npx tsc"
  },
  "openFiles": ["prisma/schema.prisma", "example.ts"]
}
