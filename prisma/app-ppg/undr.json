{
  "$schema": "https://underscore.is/schemas/undr.json",
  "scripts": {
    "install": "npm ci && npx prisma generate",
    "dev": "npx ts-node src/queries.ts",
    "build": "npx tsc"
  },
  "openFiles": ["prisma/schema.prisma", "src/queries.ts"]
}
